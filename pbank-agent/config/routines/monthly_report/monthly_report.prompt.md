[Automated monthly report — no reply expected]

Generate my cashflow summary for {LABEL}. Use the history tool with since={SINCE} and a high limit (e.g. 200) to pull that month's transactions, then send a short, friendly recap: total money in, total out, net change, and the few biggest categories or notable movements.

Keep it to a handful of lines, no markdown tables. Don't ask me anything — just send the summary. If there were no transactions, say so in one line.
